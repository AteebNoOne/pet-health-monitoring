# AI Pet Monitoring Backend

## Setup
1. Create virtual environment:
   python -m venv venv

2. Activate:
   venv\Scripts\activate  # Windows
   source venv/bin/activate  # Mac/Linux

3. Install dependencies:
   pip install -r requirements.txt
4. Run:
   python -m app.main
